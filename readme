# create a build using 

python3 -m eel main.py web --onefile --noconsole --name=Tracker
python pyinstaller.py --name=studio

# install requirement

pip3 install - r requirement.txt

# run code

python3 main.py